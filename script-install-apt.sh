
# updates repositories
# removes firefox
# upgrades existing packages
# installs google chrome
# installs discord
# installs steam
# installs flameshot, a screenshot taking tool

sudo apt update && sudo apt remove -y firefox && sudo apt upgrade -y && sudo apt -y install google-chrome && sudo apt install -y discord && sudo apt install -y steam && sudo apt install -y flameshot
